spring:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
  datasource:
    url: jdbc:h2:~/nearlyDB;AUTO_SERVER=TRUE;DATABASE_TO_UPPER=false;INIT=CREATE SCHEMA IF NOT EXISTS nearlyDB
    driverClassName: org.h2.Driver
    username: test
    password: test